node_modules/
target/
dist/
*.log
.DS_Store
Thumbs.db

# Agent edit snapshots (undo). Local, disposable.
.idexal/

# Subagent worktrees — transient, never part of the project.
.claude/
.freebuff/

# Packaged output. Installers are build products, not sources — they are
# published as release assets, never committed.
app/release/
app/release-out/
