{
	"name": "idexal",
	"version": "0.1.0",
	"description": "Idexal — a free, open, agentic coding platform (AI Core + cloud provider + VS Code extension).",
	"private": true,
	"type": "module",
	"license": "MIT",
	"engines": {
		"node": ">=22.6.0"
	},
	"scripts": {
		"setup": "node ai-core/bin/idexal-ai.js setup",
		"test": "cd ai-core && node src/test/core.test.ts"
	}
}
