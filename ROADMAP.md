# Idexal — خارطة الطريق

> إعادة البناء بـ Rust + Electron بدأت 2026-08-03. راجع
> [ADR 0001](docs/adr/0001-pivot-to-rust-electron.md).

## ✅ ما اكتمل

### النواة (`core/`, Rust — 33 اختباراً)
| المكوّن | الحالة |
|---|---|
| مزودون قابلون للتخصيص بالكامل (ملف إعدادات، بلا قائمة بيضاء) | ✅ |
| Fallback مع cooldown تصاعدي (15ث ← 5د) | ✅ |
| Anthropic (Messages API) + أي endpoint متوافق مع OpenAI | ✅ |
| حلقة استدعاء أدوات كاملة | ✅ |
| أدوات: قراءة/كتابة/سرد/أوامر + حماية تجاوز المسار | ✅ |
| ذاكرة طويلة الأمد (SQLite) بدعم عربي | ✅ |
| تعدد وكلاء: مخطط ← منفذون **متوازون فعلياً** ← مراجع | ✅ |

### بيئة العمل (`app/`, Electron + Monaco)
| المكوّن | الحالة |
|---|---|
| فتح مجلد + شجرة ملفات | ✅ |
| تبويبات + فتح/حفظ (Ctrl+S) + مؤشر تعديل | ✅ |
| طرفية مدمجة | ✅ (stdio، ليست PTY كاملة) |
| Git: حالة + فروق ملوّنة | ✅ (قراءة فقط) |
| معاينة حية / متصفح مدمج | ✅ |
| لوحة وكيل + قائمة مهام حية | ✅ |
| فحص أنواع إلزامي قبل البناء | ✅ |

### الطرفية (`cli/`)
`idexal "<مهمة>"` · `idexal agent` · `idexal review` · `idexal providers` · `idexal memory` ✅

---

## ⚠️ غير مُتحقَّق منه بعد

**استجابة نموذج ناجحة كاملة.** بيئة التطوير الحالية بلا مفتاح API صالح وبلا Ollama
محلي، فمسارات الفشل وحدها هي التي جرت فعلياً. كل ما سبق مُختبَر بنيوياً ووحدوياً،
لكن التدفق الكامل (طلب → رد → أداة → رد نهائي) ينتظر مفتاحاً حقيقياً.

---

## المراحل التالية

### أولوية عالية
- [ ] **تشغيل تجربة كاملة بمفتاح حقيقي** والتحقق من كل مسار نجاح.
- [ ] Git: عمليات تعديل (إضافة/إيداع) مع تأكيد صريح في الواجهة.
- [ ] طرفية PTY حقيقية (تتطلب وحدة أصلية) لدعم البرامج التفاعلية.
- [ ] Diff مرئي لتعديلات الوكيل مع قبول/رفض لكل تغيير.

### متوسطة
- [ ] استرجاع دلالي للذاكرة (Embeddings) — الجدول مُجهَّز في المخطط.
- [ ] تصحيح أخطاء (Debug Adapter Protocol).
- [ ] عوامل لغة Monaco (إكمال تلقائي، تشخيصات).
- [ ] تعدد مهام متزامنة (أكثر من محادثة وكيل في آن).
- [ ] وكلاء فرعيون (subagents) يستدعيهم المنفّذ.

### التوزيع
- [ ] حزم Windows/macOS/Linux (Electron Builder).
- [ ] تحديث تلقائي.
- [ ] موقع idexal.com.

## بند دائم
كل قرار معماري يُسجَّل كـ ADR في `docs/adr/`، وكل تغيير جوهري في `CHANGELOG.md`.
