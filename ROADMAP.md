# Idexal — خارطة الطريق

> إعادة البناء بـ Rust + Electron بدأت 2026-08-03. راجع
> [ADR 0001](docs/adr/0001-pivot-to-rust-electron.md). التصميم المرجعي في
> [docs/design/](docs/design/README.md)، ومخططات التنفيذ في [docs/plans/](docs/plans/README.md).

## ✅ ما اكتمل

### النواة (`core/`, Rust — 102 اختباراً)
| المكوّن | الحالة |
|---|---|
| مزودون قابلون للتخصيص بالكامل (ملف إعدادات، بلا قائمة بيضاء) | ✅ |
| Fallback مع cooldown تصاعدي (15ث ← 5د) | ✅ |
| Anthropic (Messages API) + أي endpoint متوافق مع OpenAI | ✅ |
| حلقة استدعاء أدوات كاملة | ✅ |
| **أدوات**: read/write/list/run + **search_files** (regex) + **find_files** + **edit_file** (تعديل جراحي) | ✅ |
| حماية تجاوز المسار + طبقة أدوات للقراءة فقط | ✅ |
| ذاكرة طويلة الأمد (SQLite) — **تطبيع وجذر عربي**، نطاق بجذر المستودع | ✅ |
| تعدد وكلاء: مخطط ← منفذون **متوازون فعلياً** ← مراجع | ✅ |
| **وكلاء فرعيون** (`delegate`) بعمق محدود ووراثة صلاحيات | ✅ |
| **جلسات محادثة** — متابعة حقيقية تعيد تشغيل التاريخ بأدواته | ✅ |
| **نقاط استرجاع** — لقطة قبل كل تعديل + تراجع تدريجي مستقل عن Git | ✅ |
| **دفتر استهلاك** — رموز وتكلفة وزمن لكل نداء، للوكيل الواحد **وللمسار متعدد الوكلاء** | ✅ |
| **تثبيت مزود/نموذج لكل مهمة** (`--provider`/`--model`) — يوقف التبديل التلقائي عمداً | ✅ |

### بيئة العمل (`app/`, Electron + Monaco)
| المكوّن | الحالة |
|---|---|
| **تخطيط ZCode**: شريط مهام، شاشة ترحيب، مؤلّف طلب | ✅ |
| مُحدِّد النموذج في المؤلِّف **يثبّت المزود فعلياً** للمهمة (لا إعلامي) | ✅ |
| **رقاقة الوكلاء**: تلقائي / وكيل واحد / تعدد وكلاء — اختيار صريح للتوازي | ✅ |
| `idexal <مجلد>` أو `IDEXAL_WORKSPACE` يبدأ والمشروع مفتوح | ✅ |
| مهام متعددة متزامنة + خطة حية جانبية | ✅ |
| أوضاع الصلاحيات (كاملة / تخطيط / قراءة فقط) موصولة بسلوك النواة | ✅ |
| فتح مجلد + شجرة ملفات + تبويبات + حفظ (Ctrl+S) | ✅ |
| **طرفية PTY حقيقية** (node-pty + xterm.js): ألوان، Ctrl-C، تغيير حجم | ✅ |
| **Git**: حالة + فروق ملوّنة + **إضافة/إلغاء/إيداع بتأكيد صريح** | ✅ |
| معاينة حية / متصفح مدمج | ✅ |
| صفحة إعدادات كاملة بتنقّل جانبي + إدارة مزودين واختبار اتصال | ✅ |
| **صفحة استهلاك**: إجماليات، اتجاه يومي، تفصيل بالمزوّد، أحدث النداءات | ✅ |
| **لوحة تراجع** في الرصيف: تسرد اللقطات، تتراجع خطوة، وتُحدِّث التبويب المفتوح | ✅ |
| **مصحّح أخطاء** JS/Node: نقاط توقف بعلامة على الهامش، خطوات، مكدّس، متغيّرات، مخرجات | ✅ |
| **خدمات لغة Monaco**: تشخيصات وإكمال تلقائي (TypeScript/JSON/CSS/HTML) | ✅ |
| **أيقونات SVG احترافية** (بلا رموز، بلا CDN) | ✅ |
| فحص أنواع إلزامي قبل البناء | ✅ |

### الطرفية (`cli/`)
`idexal "<مهمة>"` · `agent` · `review` · `providers` · `memory` · `grep` · `find` ✅
وفي النواة أيضاً: `sessions` · `undo` · `tool` ✅

---

## ✅ تحقّق بمزوّد حقيقي — 2026-08-04

جرى التشغيل الكامل على بوابة موحّدة محلية (٩٢ نموذجاً خلف نقطة واحدة):

| المسار | الدليل |
|---|---|
| طلب → نموذج → أداة → ملف على القرص | `hello.py` كُتب ثم قُرئ ثم شُغِّل فعلاً |
| تبديل تلقائي بين المزودين | مزوّد معطوب عمداً في المقدمة → انتقل للتالي ونجح |
| تعدد وكلاء متوازٍ | مخطّط ← منفّذون بالتوازي ← مراجع؛ ٣ ملفات، واختباراتها تمر (خرج 0) |
| تثبيت مزود/نموذج | سطر أوامر العملية الفعلي يحمل `--provider`/`--model`، والبقية لا تُجرَّب |
| دفتر الاستهلاك | ١٧ نداءً تحت `task_id` واحد، ٤٩٢٥٤ رمزاً، تكلفة محسوبة |
| نقاط الاسترجاع | لقطة تلقائية قبل كل كتابة، والملف المُنشَأ يُعلَّم `created` |

**تكامل مستمر على ثلاث منصات** (`.github/workflows/ci.yml`): اختبارات Rust
وفحص أنواع وبناء للتطبيق على ويندوز ولينكس وماك عند كل دفعة. أول تشغيل كشف
ثغرة تجاوز مساحة العمل على POSIX — انظر CHANGELOG.

**فرع التراجع إلى stdio في الطرفية — جُرِّب أخيراً، ووُجد ناقصاً.** بإزاحة الوحدة
الأصلية جانباً: الصدفة تبدأ وتظهر لافتتها ومحثّها، لكن **الأوامر المكتوبة لا
تُنفَّذ** على ويندوز. ثلاثة عيوب حقيقية أُصلحت في الطريق (السطر الجديد لا يُرسَل
لأن xterm يبعث CR وحده، ولا صدى للحروف لأن الأنبوب بلا terminal echo، والكتابة
كانت تُبلّغ بالنجاح دون أن تكتب شيئاً) — لكن المسار **ما يزال غير عامل**، ولا
أدّعي خلاف ذلك. أثره العملي محدود: بنية win32-x64 موجودة دائماً فلا يُسلك هذا
الفرع على ويندوز أصلاً؛ خطره الحقيقي على منصة بلا بنية جاهزة.

---

## المراحل التالية

### مخططات جاهزة
- [ ] [003 — تعدد مهام متزامنة](docs/plans/003-multi-task.md) (الواجهة جاهزة؛ النواة تحتاج جلسات)
- [x] [005 — طرفية PTY حقيقية](docs/plans/005-pty-terminal.md) — منجز (بنية جاهزة N-API، بلا تصريف)

### أولوية عالية
- [x] تشغيل تجربة كاملة بمزوّد حقيقي — تمّ (انظر أعلاه).

### متوسطة
- [ ] استرجاع دلالي للذاكرة (Embeddings) — الجدول مُجهَّز في المخطط.
- [ ] أدوار وكلاء متخصصة قابلة للتعريف من الإعدادات.
- [ ] تصحيح أخطاء للغات أخرى عبر DAP (بايثون/Rust). مسار JS/Node منجز عبر مفتّش V8.
- [x] **المهارات** — مطالبات محفوظة في `~/.idexal/skills.json`، تُدرج عند المؤشر.
- [x] **الأتمتة** — جدولة المهام داخل التطبيق، مع «شغّل الآن» ونتيجة آخر تشغيل.
      تعمل بينما البرنامج مفتوح فقط، وهذا معلن في الصفحة نفسها.
- [ ] صفحات إعدادات إضافية: إضافات، MCP.

### التوزيع
- [x] **حزم ويندوز** (Electron Builder): مثبِّت NSIS ونسخة محمولة بملف واحد.
      المحرّك يُشحن في `resources/core/` ويُبحث عنه هناك أولاً. مُتحقَّق منه
      بتشغيل الملف المحمول: المحرّك والمحرِّر والطرفية والمصحّح كلها تعمل بلا تثبيت.
- [ ] حزم macOS/Linux — الإعداد مكتوب (dmg/AppImage) لكن لم يُبنَ ولم يُجرَّب:
      الجهاز المتاح ويندوز فقط.
- [x] أيقونة تطبيق مولَّدة من علامة الواجهة نفسها (`npm run icon`).
- [ ] توقيع رقمي — بدونه يعرض ويندوز تحذير SmartScreen عند أول تشغيل.
- [ ] تحديث تلقائي · موقع idexal.com.

## بند دائم
كل قرار معماري يُسجَّل كـ ADR في `docs/adr/`، وكل تغيير جوهري في `CHANGELOG.md`.
