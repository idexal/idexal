# سجل التغييرات | Changelog

## [Unreleased] — 2026-08-03

### تغيير جذري | Breaking
- التخلي عن فرع VS Code (Code OSS) والبناء من الصفر بـ Electron + Rust. راجع
  [ADR 0001](docs/adr/0001-pivot-to-rust-electron.md).

### أُضيف | Added

**النواة (`core/`, Rust):**
- **محرك وكيل حقيقي** (`agent.rs`): حلقة استدعاء أدوات كاملة — يرسل، ينفّذ ما يطلبه
  النموذج من أدوات، يعيد النتائج، يكرر — بحد أقصى `maxToolRounds`.
- **مزودون قابلون للتخصيص بالكامل** (`config.rs`): أي endpoint متوافق مع OpenAI يُضاف
  في `~/.idexal/config.json` بأولويته ونموذجه وترويساته وحقول جسم الطلب الخاصة به.
  بلا قائمة بيضاء للمزودين، بلا إعادة تصريف. إدخالات الملف تعلو على المشتقّة من
  متغيرات البيئة بنفس الـ id (يمكن توجيه `anthropic` لبوابتك الخاصة).
- **Fallback حقيقي بتراجع أسّي** (`providers/mod.rs`): تجربة المزودين بالأولوية، مع
  cooldown يتضاعف (15 ثانية ← 5 دقائق) حسب الفشل المتتالي.
- **مزودان**: Anthropic (Messages API الأصلية) وOpenAI-compatible (يخدم OpenAI،
  OpenRouter، Groq، DeepSeek، Ollama، LM Studio، أي بوابة خاصة).
- **أدوات الوكيل** (`tools.rs`): `read_file`, `write_file`, `list_dir`, `run_command`
  مع حماية تجاوز المسار، وطبقة أدوات للقراءة فقط لأوضاع المراجعة.
- **ذاكرة طويلة الأمد** (`memory.rs`): SQLite دائم — حقائق، قرارات، تفضيلات، جلسات.
  استرجاع لغوي بتداخل الكلمات + دفعة حداثة، يُحقن في system prompt. **يدعم العربية
  فعلياً** (اختبار مخصص يثبت ذلك).
- أوامر `memory stats|recall|add` و`providers` في النواة.
- **تثبيت مزود/نموذج لكل مهمة**: `--provider <id>` و`--model <name>` على `stream`
  و`agent`. التثبيت **يُطفئ التبديل التلقائي عمداً**: من يختار مزوده المحلي غالباً
  يفعل ذلك كي لا يغادر كوده الجهاز، و«المحلي كان معطلاً فأرسلتُه إلى واجهة سحابية»
  خطأ لا يُستدرك. مُعرِّف مجهول = خطأ يسمّي المتاح، لا سقوط صامت إلى الافتراضي.
- **دفتر الاستهلاك صار يشمل المسار متعدد الوكلاء**: كان موصولاً بالوكيل الواحد فقط،
  فالمسار الأغلى (مخطط ← منفذون ← مراجع) لا يسجّل شيئاً. كل وكيل يفتح مقبضه الخاص
  (لأن `Connection` ليست `Sync`)، وكلهم تحت `task_id` واحد فيُجمع الجري في سطر واحد.

**واجهة الأوامر (`cli/`):**
- أمر `idexal` يعمل من أي طرفية: `idexal "<مهمة>"`, `idexal review`, `idexal providers`.
  غلاف Node بلا تبعيات حول ثنائي النواة — كل السلوك في النواة، فالطرفية والتطبيق
  يتشاركان تطبيقاً واحداً وعقداً واحداً (NDJSON).

**التطبيق (`app/`):**
- عرض أحداث `tool-call`/`tool-result` في سجل الوكيل.
- **المهارات**: مطالبات محفوظة يستدعيها المستخدم من الشريط الجانبي — تُدرَج في
  المؤلِّف **عند موضع المؤشر** لا فوق ما كتبه. تُخزَّن في `~/.idexal/skills.json`
  بجانب بقية إعداداته: يقرأها ويحرّرها وينسخها احتياطياً بلا التطبيق، كما تفعل
  المزودات. أربع مهارات مبدئية تُزرع مرة واحدة ثم تصير ملكه — لا تُعاد زراعتها،
  وإلا لعادت المحذوفة.
- **زر الإرفاق** كان بلا أي مستمع: ينقر المستخدم فلا يحدث شيء. الآن يفتح منتقي
  ملفات ويُدرج `@مسار` نسبياً لمساحة العمل. الملفات خارج المساحة تُستبعد **مع
  إخبار المستخدم بعددها**، لأن الوكيل لا يستطيع قراءتها أصلاً فالصمت يتركه ينتظر
  لا شيء.
- **أيقونة التطبيق** مولَّدة من نفس مسار SVG الذي ترسمه الواجهة
  (`build/make-icon.py`)، فلا تنفصل الأيقونة عن العلامة داخل البرنامج. سبع مقاسات
  في ملف `.ico` واحد لأن شريط المهام وشريط العنوان وقائمة الملفات يختار كلٌّ منها
  مقاساً مختلفاً، والمقاس الغائب يُقاس بتشويه لا يُتخطّى.
- **حزم قابلة للتثبيت** (`npm run package`): مثبِّت NSIS، و**نسخة محمولة بملف واحد**
  لا تحتاج تثبيتاً أصلاً — أقرب لروح «حر وبلا عوائق». المحرّك (`idexal-core`) يُشحن
  في `resources/core/` بجانب التطبيق لا داخل الأرشيف، لأنه ثنائي مستقل يجب أن يبقى
  قابلاً للتنفيذ، و`resolveCoreBinary()` يبحث هناك **أولاً** فلا تلتقط نسخة مثبَّتة
  شجرة بناء قديمة على الجهاز نفسه. مُتحقَّق منه بتشغيل الملف المحمول فعلياً: المحرّك
  والمحرِّر والطرفية والمصحّح تعمل كلها بلا تثبيت.
- **مصحّح أخطاء حقيقي** لـ JavaScript/Node: نقاط توقف بالنقر على هامش المحرِّر،
  إيقاف/متابعة/تخطّي/دخول/خروج، مكدّس استدعاء، ومتغيّرات النطاق المحلي والمغلق،
  ومخرجات البرنامج. يقود مفتّش V8 عبر CDP بدل بروتوكول DAP: كل مُهايئ DAP ثنائي
  يجب على المستخدم تثبيته أولاً، بينما مفتّش V8 موجود داخل `node` أصلاً — فيعمل
  التصحيح لحظة فتح البرنامج بلا أي تثبيت. سطح الـ IPC مصمَّم بشكل DAP نفسه
  (بدء/متابعة/خطوة/نقاط توقف/مكدّس/متغيّرات) كي يُضاف مُهايئ DAP لاحقاً بلا
  تغيير الواجهة. **النطاق بصراحة:** JavaScript فقط؛ بايثون وRust يحتاجان DAP.
- **صفحة الاستهلاك**: إجماليات، اتجاه يومي، تفصيل بالمزوّد، وأحدث النداءات
  بأخطائها. النموذج بلا سعر معروف يُكتب «سعر غير معروف» لا `$0.0000` — رقم
  مُختلق أمام المستخدم أسوأ من فراغ صريح.
- **لوحة تراجع** في الرصيف: تسرد اللقطات، وتميّز «أُنشئ» عن «عُدّل» (التراجع عن
  الأول حذف)، وبعد كل تراجع **تُحدِّث التبويب المفتوح**. الأخير هو الأخطر لو
  أُهمل: تبويب يعرض نصاً قديماً، وأول `Ctrl+S` يعيد تطبيق ما تراجعتَ عنه للتو.
  المسح النهائي يستأذن، كالإيداع.
- **رقاقة الوكلاء**: تعدد الوكلاء كان يُختار سرّاً بطول النص (أكثر من ٦٠ حرفاً)،
  فلا سبيل لطلب التوازي أصلاً — وجملة قصيرة قد تصف عملاً لخمسة وكلاء. الآن
  اختيار صريح: تلقائي / وكيل واحد / تعدد وكلاء.
- **`idexal <مجلد>` و`IDEXAL_WORKSPACE`**: يبدأ التطبيق والمشروع مفتوح، كما يفتح
  المحرِّر المجلد الذي شُغِّل منه.
- **مُحدِّد النموذج في المؤلِّف صار حقيقياً**: كان إعلامياً بحتاً — يغيّر النص ولا شيء
  غيره. الآن يمرّر `--provider`/`--model` للنواة، والرقاقة تُعلَّم بصرياً عند التثبيت
  لأن إطفاء التبديل التلقائي تغيّر سلوكي يجب أن يُرى دون فتح القائمة.
- **طرفية PTY حقيقية** بدل stdio المأنبوب: `@lydell/node-pty` (بنى N-API جاهزة —
  لا تصريف ولا `electron-rebuild`) في العملية الرئيسية، وxterm.js في الواجهة.
  ألوان ANSI، Ctrl-C يوقف المهمة الأمامية، وقناة `terminal:resize` تُبقي حجم
  الـ PTY مطابقاً للشبكة المعروضة. تراجع تلقائي إلى stdio إن غابت البنية.
- إنهاء الطرفية عند الخروج يستخدم `taskkill /T` على ويندوز: `kill()` في node-pty
  غير متزامن (يُفرِّع مساعداً لحصر عمليات الكونسول أولاً)، فكان التطبيق يخرج قبل
  موت الصدفة — رُصد فعلياً: `cmd.exe` بقي حياً بعد إغلاق النافذة.

### أُصلح | Fixed

**أربعة أعطال ظهرت عند أول تشغيل بمزوّد حقيقي** (بوابة موحّدة محلية، ٩٢ نموذجاً).
كلها كانت تجعل الوكيل يبدو عاجزاً وهو قادر:

- **`run_command` بلا مهلة، ويعلّق إلى الأبد.** `.output()` ينتظر إغلاق الأنابيب،
  وأي برنامج رسومي أو خادم تطوير يرث تلك الأنابيب ويمسكها ما دام حياً. رُصد فعلياً:
  فتح Notepad علّق النواة أكثر من ٢٥ ثانية حتى قُتلت. الآن: مهلة افتراضية ١٢٠ث
  (حتى ٩٠٠) مع قتل شجرة العمليات، و`"background": true` لما لا ينتهي وحده —
  بمخرجات مفصولة تماماً فلا يبقى ما يُورَّث.
- **الخلفية كانت تُبلغ بالنجاح دائماً.** الصدفة تبدأ ثم تفشل، فيظن النموذج أن
  المتصفح فُتح. الآن: مهلة سماح قصيرة ثم فحص حالة الخروج — الخروج الفوري بفشل
  يُبلَّغ فشلاً صريحاً.
- **التعليمات لم تذكر نظام التشغيل.** فخمّن النموذج يونكس: على ويندوز أنتج
  `google-chrome &`. الآن يُحقن سطر بيئة (نظام، صدفة، مسار العمل) في تعليمات
  الوكيل الواحد والمنفّذين معاً.
- **التطبيق بلا مجلد مفتوح كان يشغّل الوكيل داخل مجلد Idexal نفسه** — يكتب ملفات
  حقيقية هناك بينما المستخدم ينتظر تغييراً في مشروعه. الآن يرفض ويطلب فتح مجلد،
  ويقبل `idexal <مجلد>` أو `IDEXAL_WORKSPACE` ليبدأ جاهزاً.

**الذاكرة طويلة الأمد كانت غير قابلة للاسترجاع عملياً — عطلان.** اكتُشفا بفحص
قاعدة بيانات المستخدم الحقيقية لا بقراءة الكود:

- **نطاق المشروع كان اسم المجلد الحالي.** وكيل يعمل في `idexal/core` يحفظ تحت
  «core»، والمستخدم في جذر المستودع يبحث في «idexal» فلا يجد شيئاً. كل مجلد فرعي
  صار جزيرة. النطاق الآن هو **جذر المستودع** (`.git` بالصعود)، لأن ذلك ما يعنيه
  المطوّر بـ«هذا المشروع». مجلد بلا مستودع يبقى باسمه.
- **الكلمات المفتاحية القديمة مفصولة بفواصل** (`always,use,tabs`) بينما الاسترجاع
  يقسم بالمسافات فقط — فكل ذكرى منقولة من متجر Node السابق كانت طوباً واحداً لا
  يطابق شيئاً. القسمة الآن على المسافات **والفواصل**.

**واسترجاع العربية كان مطابقة حرفية.** الادعاء بدعم العربية كان أجوف: `الإعدادات`
و`الاعدادات` كلمة واحدة للقارئ وكلمتان للمقارنة النصية، و`المزودين` لا تشترك بحرف
واحد في طرفيها مع `مزود`. أُضيف تطبيع (تشكيل، تطويل، صور الألف، ة/ه، ى/ي) وجذر
خفيف للسوابق واللواحق بحد أدنى ٣ أحرف — فالإفراط في الجذر يخلط كلمات لا علاقة
بينها. الكلمات المخزّنة **تُطبَّع عند القراءة** أيضاً، وإلا لفقد المستخدم كل ذكرى
كتبها قبل هذا التغيير. وأُضيف `memory rescope <من> <إلى>` أمراً صريحاً: أي ذكرى
تخصّ أي مشروع ليس شيئاً يمكن للمتجر استنتاجه، والتخمين كان سيدمج أعمالاً غير
مترابطة بصمت.

**صحّة المزودين صارت مشتركة عبر الجرية متعددة الوكلاء.** كانت خاصة بكل وكيل،
فيعيد كلٌّ منهم اكتشاف المزوّد الميت من جديد: قياس فعلي على بوابة حقيقية أظهر
**سبع محاولات** لمزوّد معطوب في جرية واحدة، واحدة لكل وكيل، كلٌّ تدفع رحلة كاملة.
الصحّة معرفة عن العالم لا حالة وكيل. بعد الإصلاح: **ثلاث محاولات** لا واحدة —
لأن مهلة التهدئة (١٥ث) تنتهي أثناء جرية تستغرق دقائق، والمزوّد الذي قد يكون تعافى
يستحق إعادة محاولة. هذا هو التصميم يعمل، لا نقصٌ فيه.

**ومحرِّر الأكواد كان يرسم نصّه خارج الشاشة — ١٦ مليون بكسل.** الصفحة `dir="rtl"`،
وMonaco يضع طبقاته بإحداثيات مطلقة تعكسها حاوية RTL. القياس: سطر مرسوم فعلاً،
بمحتواه ولونه الصحيحين، عند `x = 16,776,637`. فالمحرِّر يبدو فارغاً تماماً، ومعه
كل ما يضعه Monaco في الهامش (أرقام الأسطر، الزخارف، علامات نقاط التوقف). ولمّا أُصلح، عادت
كلها معاً: النص، وأرقام الأسطر، وعلامة نقطة التوقف الحمراء على الهامش. العلاج
`direction: ltr` على حاوية المحرِّر — والكود يُكتب من اليسار لليمين مهما كانت لغة
الواجهة، فهذا صواب بذاته لا مجرّد التفاف. **درس منهجي:** فحوصي السابقة قرأت
الـ DOM والنموذج فوجدتهما سليمين، فاستنتجتُ أن المحرِّر يعمل. النص كان في الـ DOM
فعلاً — لكن خارج الشاشة. لا يكفي أن يوجد العنصر؛ يجب أن يُقاس **أين رُسم**.

**وقبل ذلك، لم يكن Monaco يُحمَّل من الأساس.** إقلاعه كان `<script>` مضمّناً، وسياسة
أمان المحتوى عندنا `script-src 'self' 'unsafe-eval'` بلا `'unsafe-inline'` — فكان
المتصفح يرفضه بصمت، ولم يُفتح أي ملف في المحرِّر قط. نُقل الإقلاع إلى ملف مستقل؛
تليين السياسة لأجل ثلاثة أسطر كان المقايضة الخاطئة. وحاجزان آخران في الطريق نفسه:
السياسة كانت ترفض عامل Monaco المُنشأ من blob (أُضيف `worker-src 'self' blob:`
وحده، لا توسيع `script-src` الذي كان سيجعل أي نص مُحقَن نصاً قابلاً للتنفيذ)،
ومسار AMD كان يُحلّ إلى **جذر نظام الملفات** تحت `file://` فيطلب العامل
`file:///vs/...`. **تنبيه صريح:** خدمات اللغة (تشخيصات، إكمال) ما تزال معطلة —
التلوين النحوي يعمل، لكن `getWorkerUrl` لا يُستدعى أصلاً. التشخيص الكامل مُدوَّن
في خارطة الطريق. وأُخفي مجلد `.idexal` (مخزن
اللقطات) من شجرة الملفات — دفاترنا لا مكان لها في مشروع المستخدم.

وأيضاً: **التعليمات كانت تجعل النموذج يعتذر بدل أن ينفّذ** — تصف نفسها كأداة نصية
فقط، فيردّ «لا أستطيع فتح متصفح» وهو يملك صدفة كاملة. صار نصّها يقول صراحة إن
`run_command` صدفة حقيقية على جهاز المستخدم، وإن الأصل التنفيذ لا السؤال.

**هدفا ويندوز كانا يكتبان الملف نفسه.** كلاهما يُنتج `.exe`، و`artifactName`
مشترك يعني أن الثاني يستبدل الأول **بصمت**: إصدار يَعِد بملفين ويشحن واحداً. رُصد
فعلياً — النسخة المحمولة محت المثبِّت. لكل هدف اسمه الآن.

**ثلاثة أخطاء حقيقية أخرى** اكتُشفت بالتشغيل الفعلي لا بالقراءة، ولكلٍّ منها اختبار انحدار:
- **المزودون كانوا يُجرَّبون مرتين** في كل دور: الفشل يضع المزود في cooldown فيلتقطه
  المرور الثاني — مضاعفة زمن الانتظار وتكرار كل خطأ.
- **ملف الإعدادات المُوثَّق لم يكن ليُحلَّل أصلاً**: JSON بـ camelCase (`baseUrl`,
  `apiKeyEnv`) بينما البنى تتوقع snake_case — كل مزود يضيفه المستخدم كان سيُهمَل
  صامتاً. الاختبار الآن يحلّل ملف المثال الحقيقي لا نسخة مكتوبة يدوياً.
- **الذاكرة كانت تنهار على قواعد البيانات الموجودة مسبقاً**: المخطط الجديد استخدم
  `kind` بينما المتجر السابق (Node) يستخدم `type` — الاختبارات على ملفات مؤقتة جديدة
  كانت تنجح دوماً، لكن فتح `~/.idexal/memory.db` حقيقي كان يفشل ويُسقِط الذاكرة كلياً.
  المخطط الآن مطابق للقديم فتُحفظ ذاكرة المستخدم السابقة وتُستخدم.

### التحقق | Verification
- 104/104 اختبار وحدة ناجح، بناء نظيف بلا تحذيرات.
- الذاكرة مُختبَرة على قاعدة البيانات الحقيقية للمستخدم: الذاكرة السابقة نجت،
  والإضافة والاسترجاع بالعربية يعملان.
- **جرى التشغيل الكامل بمزوّد حقيقي** (2026-08-04، بوابة موحّدة محلية): طلب ←
  نموذج ← أداة ← ملف على القرص، وتبديل تلقائي، وتعدد وكلاء متوازٍ أنتج ملفات
  واختباراتها تمر، ودفتر استهلاك بأرقام حقيقية. التفصيل في `ROADMAP.md`.
- **ما زال بلا تحقّق**: غير ويندوز، وفرع التراجع إلى stdio في الطرفية، وخدمات
  لغة Monaco (تشخيصات/إكمال).

### بيئة التطوير | Dev environment
- تثبيت Rust toolchain (`rustup`, stable-msvc).
- محلل DNS المحلي غير مستقر لنطاقات جديدة (`index.crates.io`, `artifacts.electronjs.org`):
  الحل تسخين مسبق + إعادة محاولة، لا تغيير دائم في الشبكة.
