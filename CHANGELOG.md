# سجل التغييرات | Changelog

## [Unreleased] — 2026-08-03

### تغيير جذري | Breaking
- التخلي عن فرع VS Code (Code OSS) والبناء من الصفر بـ Electron + Rust. راجع
  [ADR 0001](docs/adr/0001-pivot-to-rust-electron.md).

### أُضيف | Added

**النواة (`core/`, Rust):**
- **محرك وكيل حقيقي** (`agent.rs`): حلقة استدعاء أدوات كاملة — يرسل، ينفّذ ما يطلبه
  النموذج من أدوات، يعيد النتائج، يكرر — بحد أقصى `maxToolRounds`.
- **مزودون قابلون للتخصيص بالكامل** (`config.rs`): أي endpoint متوافق مع OpenAI يُضاف
  في `~/.idexal/config.json` بأولويته ونموذجه وترويساته وحقول جسم الطلب الخاصة به.
  بلا قائمة بيضاء للمزودين، بلا إعادة تصريف. إدخالات الملف تعلو على المشتقّة من
  متغيرات البيئة بنفس الـ id (يمكن توجيه `anthropic` لبوابتك الخاصة).
- **Fallback حقيقي بتراجع أسّي** (`providers/mod.rs`): تجربة المزودين بالأولوية، مع
  cooldown يتضاعف (15 ثانية ← 5 دقائق) حسب الفشل المتتالي.
- **مزودان**: Anthropic (Messages API الأصلية) وOpenAI-compatible (يخدم OpenAI،
  OpenRouter، Groq، DeepSeek، Ollama، LM Studio، أي بوابة خاصة).
- **أدوات الوكيل** (`tools.rs`): `read_file`, `write_file`, `list_dir`, `run_command`
  مع حماية تجاوز المسار، وطبقة أدوات للقراءة فقط لأوضاع المراجعة.
- **ذاكرة طويلة الأمد** (`memory.rs`): SQLite دائم — حقائق، قرارات، تفضيلات، جلسات.
  استرجاع لغوي بتداخل الكلمات + دفعة حداثة، يُحقن في system prompt. **يدعم العربية
  فعلياً** (اختبار مخصص يثبت ذلك).
- أوامر `memory stats|recall|add` و`providers` في النواة.

**واجهة الأوامر (`cli/`):**
- أمر `idexal` يعمل من أي طرفية: `idexal "<مهمة>"`, `idexal review`, `idexal providers`.
  غلاف Node بلا تبعيات حول ثنائي النواة — كل السلوك في النواة، فالطرفية والتطبيق
  يتشاركان تطبيقاً واحداً وعقداً واحداً (NDJSON).

**التطبيق (`app/`):** عرض أحداث `tool-call`/`tool-result` في سجل الوكيل.

### أُصلح | Fixed
ثلاثة أخطاء حقيقية اكتُشفت بالتشغيل الفعلي لا بالقراءة، ولكلٍّ منها اختبار انحدار:
- **المزودون كانوا يُجرَّبون مرتين** في كل دور: الفشل يضع المزود في cooldown فيلتقطه
  المرور الثاني — مضاعفة زمن الانتظار وتكرار كل خطأ.
- **ملف الإعدادات المُوثَّق لم يكن ليُحلَّل أصلاً**: JSON بـ camelCase (`baseUrl`,
  `apiKeyEnv`) بينما البنى تتوقع snake_case — كل مزود يضيفه المستخدم كان سيُهمَل
  صامتاً. الاختبار الآن يحلّل ملف المثال الحقيقي لا نسخة مكتوبة يدوياً.
- **الذاكرة كانت تنهار على قواعد البيانات الموجودة مسبقاً**: المخطط الجديد استخدم
  `kind` بينما المتجر السابق (Node) يستخدم `type` — الاختبارات على ملفات مؤقتة جديدة
  كانت تنجح دوماً، لكن فتح `~/.idexal/memory.db` حقيقي كان يفشل ويُسقِط الذاكرة كلياً.
  المخطط الآن مطابق للقديم فتُحفظ ذاكرة المستخدم السابقة وتُستخدم.

### التحقق | Verification
- 26/26 اختبار وحدة ناجح، بناء نظيف بلا تحذيرات.
- الذاكرة مُختبَرة على قاعدة البيانات الحقيقية للمستخدم: الذاكرة السابقة نجت،
  والإضافة والاسترجاع بالعربية يعملان.
- **غير مُتحقَّق منه بعد**: استجابة نموذج ناجحة كاملة. لا يوجد مفتاح API صالح ولا
  Ollama محلي في بيئة التطوير هذه، فمسارات الفشل وحدها هي التي جرت فعلياً.

### بيئة التطوير | Dev environment
- تثبيت Rust toolchain (`rustup`, stable-msvc).
- محلل DNS المحلي غير مستقر لنطاقات جديدة (`index.crates.io`, `artifacts.electronjs.org`):
  الحل تسخين مسبق + إعادة محاولة، لا تغيير دائم في الشبكة.
